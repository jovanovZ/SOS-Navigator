services:

  frontend:
    image: zix3011/sos-navigator-frontend:latest
    restart: always
    ports:
      - "3000:3000"

  backend:
    image: zix3011/sos-navigator-backend:latest
    restart: always
    ports:
      - "3002:3002"